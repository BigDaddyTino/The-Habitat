<service>
  <id>HabitatWeb</id>
  <name>Habitat Web</name>
  <description>Loopback-only Next.js application service for The Habitat.</description>
  <executable>node.exe</executable>
  <arguments>node_modules\next\dist\bin\next start -H 127.0.0.1 -p 3000</arguments>
  <workingdirectory>{{INSTALL_ROOT}}\apps\web</workingdirectory>
  <!-- Absolute on purpose. WinSW resolves a relative logpath against the service
       process's working directory, which is C:\Windows\System32 for a LocalSystem
       service, so stdout and stderr land there while only the wrapper log appears
       beside the executable. -->
  <logpath>{{INSTALL_ROOT}}\web-logs</logpath>
  <!-- sizeThreshold is in KB, not bytes: 10240 KB is the intended 10 MB. -->
  <log mode="roll-by-size-time">
    <sizeThreshold>10240</sizeThreshold>
    <keepFiles>14</keepFiles>
    <pattern>yyyyMMdd</pattern>
    <autoRollAtTime>00:00:00</autoRollAtTime>
  </log>
  <env name="NODE_ENV" value="production" />
  <stoptimeout>20sec</stoptimeout>
  <onfailure action="restart" delay="10 sec" />
  <onfailure action="restart" delay="30 sec" />
  <onfailure action="none" />
</service>
